package com.example.challengmelibootcamp.view.listener.`interface`

import com.example.challengmelibootcamp.data.model.ProductModel

interface OnClickItemListener {
    fun action(product: ProductModel)
}